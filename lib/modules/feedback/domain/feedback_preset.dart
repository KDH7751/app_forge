/// semantic feedback preset 목록.
enum FeedbackPreset {
  error,
  success,
  warning,
  info,
  confirm,
  destructiveConfirm,
  sessionExpired,
}

/// request가 사용할 의미/시각 계열.
enum FeedbackVariant {
  error,
  success,
  warning,
  info,
  confirm,
  destructiveConfirm,
  sessionExpired,
}
